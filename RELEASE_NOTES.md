# GRID STELLA — リリースノート

> **GRID STELLA（グリッドステラ）／方位観察官の天体調律盤**
> ダークファンタジー世界観のバックパック・パズル × オートバトラー。
> Next.js 14 (App Router) · TypeScript · Tailwind CSS でフルクライアントサイド構築、静的エクスポート対応。

これまでに「やったこと」と、今後「やること」を日付単位でまとめたリリースノートです。
各バージョンは `youki0p0/GRID-STELLA` のコミット／PR 履歴に対応します。

---

## 2026-06-22 — 神楽マキナ Ver0.2（回路オートバトラーへ全面リビルド）

「武器を並べる」から **「機械神の回路を構築する」** ゲームへとコンセプトを刷新した大型アップデート。

### 神楽マキナ Ver0.2（#17）
- **2層グリッド**システムを導入。コアから電力を引き回す回路構築が中核に。
- **ドラッグ&ドロップ**でのアイテム配置を刷新（`CircuitBoard.tsx`）。
- **レシピ合成（merge）** — アイテムを組み合わせて上位装置を生成（`merge.ts` / `recipes.ts`）。
- **ユニークアイテム**（`unique.ts`）と**カテゴリ分類**を追加。
- アイテム詳細パネル（`ItemDetail.tsx`）、タイル色分け（`tileTint.tsx`）を追加。
- `battle.ts` / `grid.ts` / `run.ts` / `data.ts` / `types.ts` を再設計し、テスト（`battle.test.ts` / `grid.test.ts` / `run.test.ts`）を拡充。

### 神楽マキナ リビルド + ARENA 統合（#16）
- 回路オートバトラー（神楽マキナ）の初期実装一式（`src/lib/machina/`）。
  - エネルギー（毎秒回復・武器ごとに消費）／電力接続グリッド／状態異常／バフを実装。
  - グリッド種類：通常 / シールド / クロック / バッテリー / パワー。
  - 状態異常：過電圧・ウイルス・ジャミング・フリーズ・メモリリーク・クラッシュ。
- 戦闘 FX スプライト追加（burst / crit / heal / poison / shield / slash）。
- 正準仕様書 `docs/MACHINA.md` を追加。

### ARENA — バッグ構築オートバトラー（#15）
- `/game` を **ARENA**（バッグ構築オートバトラー）として刷新。旧ループディフェンスは `/classic` に保存。
- **3ジョブ** — 衛士 Sentinel / 触媒士 Catalyst / 両替商 Broker（各3戦術）。
- **36種のアイテム**・4レアリティ・配置シナジー（隣接／行／列／盤面オーラ）+ ゴールドスケーリング。
- **2モード** — Short（10勝・3ライフ）/ Long（15勝・5ライフ）。
- **Elo ランクマッチ**（Short / Long 別ラダー、Bronze → 観測卿/Master）。
- **Supabase 非同期ゴーストPvP** — 自分の盤面を公開し、レーティング近傍の相手を取得。PvE シャドウフォールバック付き。
- 決定論的戦闘シム（`mulberry32` シード）と読みやすいバトルログ。
- 全スプライトをオリジナル生成（`scripts/gen_assets.py`）。設計ドキュメント `docs/ARENA.md` を追加。

---

## 2026-06-17 — リリース品質向上 第2弾

### Release quality 2（#13）
- **ランディングページ**を刷新（`src/app/page.tsx`）。
- **パーティクルシステム**を追加（`src/lib/merge/particles.ts` + テスト）。
- パフォーマンス最適化。
- CI に **型チェック**を追加（`.github/workflows/ci.yml`）。

### ロビーホームの再デザイン（#14）
- ロビーホームを **ボトムタブバー型レイアウト**（merge-boss 風）にリスタイル。

---

## 2026-06-16 — 初期構築 〜 コアゲームループ確立（最も大きな開発日）

ゼロからプロジェクトを立ち上げ、デザインモックアップから始めて、マージ × ループディフェンス × ローグライクのコアループとメタレイヤーまでを一気に構築。

### プロジェクト基盤
- `claude.md` 作成・更新、デザインバンドル（`gridstella.zip` / `project/` デザインシステム）を取り込み。
- **GRID STELLA プロジェクト**を Next.js 14 で立ち上げ（#1）。ショップ&バックパックのモックアップ画面、デザイントークン、コアUIコンポーネント（Badge / Button / Panel / ItemCard / StatBar）を実装。
- CI / デプロイ用 GitHub Actions、ESLint、Vitest を整備。

### コアゲームループ
- **ループディフェンス戦闘**をゲーム画面に追加（#2）。
- `/game` を **マージ × ループディフェンス × ローグライク**（タッチDnD対応）に全面リビルド（#3）。
- マージエンジンを抽出しテスト追加、売却 / 永続化 / チュートリアルを整備。

### 機能拡張（並行フィーチャーストリーム）
- **5つの並行モジュール**を統合（#4） — 難易度 / アルティメット / 敵ロスター / 実績 / オーディオ。
- **Round 2**（#5） — レリック、盤面シナジー、ターゲティングモード、スコア / エンドレス、コーデックス。
- **Round 3**（#6） — コンボ、ウェーブミューテーター、設定の永続化、ロア、数値フォーマット。
- **Round 4**（#7） — 状態異常、ショップ、イベント、プレステージメタ、ボス挙動。
- **Round 5**（#8） — ロビー / メタレイヤー（ステージ、コレクション、ガチャ、トライアル、放置収益）。

### パズルボード & ギア
- **パズルボード**（#9） — マルチセルの異形ピース、グリッド拡大、バランス調整。
- **観測装（装備パズルボード）**（#10） — ギアピースをはめてラン全体のパッシブを獲得。
- **Regalia メタ**（#11） — パーツスロット式装備（8+コア）、ジェムソケット、星アップグレード。

### リリース品質 第1弾
- **Release quality**（#12） — セーブデータ、PWA 対応、ヘルプ、reduce-motion 対応、バランス調整。

---

## 今後やること（ロードマップ）

`TODO.md` および `docs/MACHINA.md` を正準とした次の作業候補：

- **神楽マキナ MVP の仕上げ** — ショートモード / 3ジョブ / 共通＆ジョブ固有アイテム / オートバトルの完成度向上。
- **後日対応（MVP外）** — PvP通信、ランクマッチ、リプレイ、課金、シーズン、追加職業、ロングモード。
- ARENA / 神楽マキナ / classic の 3 体験の整理と導線統一。
- バランス調整とテスト網羅の継続。

---

*このリリースノートはコミット／PR 履歴をもとに自動的に整理したものです。最新の正準仕様は `docs/MACHINA.md` を参照してください。*
